import CatelogSection from "../../../../../shared/components/Catelog/CatelogSection";

const Overlays = ({ data }) => {
  return <CatelogSection cardData={data} title={"Overlays"} />;
};

export default Overlays;
