import CatelogSection from "../../../../../shared/components/Catelog/CatelogSection";

const Elements = ({ data }) => {
  return <CatelogSection cardData={data} title={"Elements"} />;
};

export default Elements;
