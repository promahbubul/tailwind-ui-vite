import CatelogSection from "../../../../../shared/components/Catelog/CatelogSection";

const Feedback = ({ data }) => {
  return <CatelogSection cardData={data} title={"Feedback"} />;
};

export default Feedback;
