import ComponentContainer from "../../../../../shared/components/ComponentContainer";

const FullWidthWithLinks = () => {
  return (
    <ComponentContainer title={"Full width with links"}></ComponentContainer>
  );
};

export default FullWidthWithLinks;
