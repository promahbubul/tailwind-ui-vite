import React from "react";
import ComponentContainer from "../../../../../shared/components/ComponentContainer";

const FullWidthOnMobileConstrainedToBreakpointWithPaddedContentAboveMobile =
  () => {
    return (
      <ComponentContainer
        title={
          "Full-width on mobile, constrained to breakpoint with padded content above mobile"
        }
      ></ComponentContainer>
    );
  };

export default FullWidthOnMobileConstrainedToBreakpointWithPaddedContentAboveMobile;
