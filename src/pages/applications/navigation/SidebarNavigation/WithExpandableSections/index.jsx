import ComponentContainer from "../../../../../shared/components/ComponentContainer";

const WithExpandableSections = () => {
  return (
    <ComponentContainer title={"With expandable sections"}></ComponentContainer>
  );
};

export default WithExpandableSections;
