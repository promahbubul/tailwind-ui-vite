import ComponentContainer from "../../../../../shared/components/ComponentContainer";

const ListWithDescription = () => {
  return (
    <ComponentContainer title={"List with description"}></ComponentContainer>
  );
};

export default ListWithDescription;
