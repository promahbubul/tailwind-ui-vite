import ComponentContainer from "../../../../../shared/components/ComponentContainer";

const WithBadgeAndDropdown = () => {
  return (
    <ComponentContainer title={"With badge and dropdown"}></ComponentContainer>
  );
};

export default WithBadgeAndDropdown;
